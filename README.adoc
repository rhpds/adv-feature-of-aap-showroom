= LB1347 - Advanced Features of Ansible Automation Platform - Automation Controller

* https://github.com/rhpds/adv-feature-of-aap-showroom[Source code]
* https://rhpds.github.io/adv-feature-of-aap-showroom[Rendered site]

== Quick Style Guide

* Use numbered lists for instructions that must be followed in order; use bullets for parallel items or general lists
* Follow https://docs.antora.org/antora-ui-default/ui-macro-styles/[Antora UI macro styles,window=blank]
** Use `menu:MenuName[SubMenu > SubSubMenu]` for menu, tabs and navigation
** Use `btn:[ButtonName]` for UI buttons and similar
** Use `kbd:[KeyName]` for key strokes
* Use `back ticks` for commands, for example `sudo` or `ansible-navigator`
* Use **Bold** for field names and `Monospace` for field values in a dialog
* Write https://asciidoctor.org/docs/asciidoc-recommended-practices/#one-sentence-per-line[one sentence per line,window=blank]
* Open external links in a new tab using `link:url[Text,window=blank]` or `https://url[Text,window=blank]` (a trailing `^` on the link text is an acceptable alternative).
Internal links to environment services have no text to not obfuscate the URL, but are still opened in a new tab.

== Nookbag Showroom Template

A template for creating Red Hat Showroom lab guides and demo content, built with https://antora.org[Antora].

* 0% cruft
* Red Hat Demo Platform UI Bundle default
* Dev Mode extension that displays the asciidoc attributes you have to work with

=== Documentation

Full documentation — getting started, content structure, configuration, local development, and AI-assisted content creation — is available at:

https://rhpds.github.io/showroom_template_nookbag/

=== Quick Start

. https://github.com/rhpds/showroom_template_nookbag/generate[Create a repo from this template] (suggested naming: `showroom_<lab-name>`)
. Clone your new repo and preview locally:
+
[source,sh]
----
podman run --rm --name antora -v $PWD:/antora:z -p 8080:8080 -i -t ghcr.io/juliaaano/antora-viewer
----
+
On SELinux systems, append `:z` to the volume mount.

. Edit content in `content/modules/ROOT/pages/`
. See the https://rhpds.github.io/showroom_template_nookbag/modules/content-repo.html[Content Repository] docs for directory layout, Podman Compose with live reload, dev mode, and more.

=== Create Content with AI

The https://github.com/rhpds/rhdp-skills-marketplace[RHDP Skills Marketplace] provides skills for Cursor IDE and Claude Code that generate labs, demos, and validate content against Red Hat standards.

See the https://rhpds.github.io/showroom_template_nookbag/modules/content-repo.html#_creating_content_with_ai_tools[AI Tools documentation] for details.
