---
- name: Solve Module N
  hosts: bastion
  gather_facts: false
  become: true
  vars:
    ansible_user: "{{ bastion_user | default('lab-user') }}"
  tasks:
    - name: Placeholder - implement solve tasks
      ansible.builtin.debug:
        msg: "Solve logic goes here"
